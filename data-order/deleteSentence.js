const connectDB = require('../database/db');
function deleteSentence() {
    // connectDB('inverted_index3', (collection) => {
    //     for(let i=0; i<493; i++) {
    //         collection.find({"title": /.*人社 2017後期.*/})
    //             .skip(i*100).limit(i*100+1).toArray((err, sort) => {
    //                 for(let k=0; k<100; k++){
    //                     collection.update()
    //                 }
    //         })
    //     }
    // });
}
deleteSentence();
